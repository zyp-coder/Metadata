本地开发：`docker-compose up --build` 一键启动 DB、Redis 与后端（端口 8000）；也可通过 `python manage.py runserver` 配合 `local_settings.py` 使用 SQLite3 快速调试。环境变量 `DJANGO_SECRET_KEY`、`DB_*`、`REDIS_*`、`AI_API_BASE`、`AI_API_KEY`、`ARCHIVE_AUTO_REFRESH_MINUTES` 控制运行时行为。