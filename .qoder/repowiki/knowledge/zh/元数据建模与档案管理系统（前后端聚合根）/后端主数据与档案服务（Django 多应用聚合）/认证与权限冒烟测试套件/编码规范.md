- 每个脚本定义统一的 BASE URL、固定测试账号（smoke_test/probe_user）和 check(name, cond, extra) 断言函数，集中输出 PASS/FAIL 并累计计数。
- 鉴权统一通过 Authorization: Token <jwt> 请求头携带，登录后从 /auth/login/ 响应中取 token 复用。
- 权限相关用例围绕 visible_codes 与 editable_codes 子集关系进行构造与校验，并以 400/403 等状态码作为断言依据。
- 测试数据具备幂等性：先查询是否存在同名角色/用户，存在则复用或清理后再创建，避免重复执行污染环境。