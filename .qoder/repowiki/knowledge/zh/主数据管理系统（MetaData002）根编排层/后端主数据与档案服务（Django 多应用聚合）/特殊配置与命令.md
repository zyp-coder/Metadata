`docker-compose up --build` 一键拉起 DB、Redis 与后端（8000 端口）；本地调试可 `python manage.py runserver` 并配合 `local_settings.py` 使用 SQLite3；环境变量 `DJANGO_SECRET_KEY`、`DB_*`、`REDIS_*`、`AI_API_BASE`、`AI_API_KEY`、`ARCHIVE_AUTO_REFRESH_MINUTES` 控制运行行为。