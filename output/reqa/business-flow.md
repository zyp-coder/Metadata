# 业务流程

---

## 流程一：域→档案创建

```mermaid
flowchart LR
    A[域管理页面] --> B{域有标准字段?}
    B -->|否| C[提示: 请先完成字段管理]
    B -->|是| D[点击新建档案]
    D --> E{该域已有档案?}
    E -->|是| F[提示: 已有档案, 跳转档案列表]
    E -->|否| G[读取 StandardField 元数据]
    G --> H[创建档案配置]
    H --> I[跳转档案列表]
```

---

## 流程二：档案数据维护

```mermaid
flowchart LR
    A[档案列表] --> B[选择档案]
    B --> C[档案详情页]
    C --> D{操作}
    D -->|新增| E[填写表单]
    D -->|编辑| F[修改数据]
    D -->|删除| G[确认删除]
    E --> H[保存+版本快照+日志]
    F --> H
    G --> H
    H --> I{是否同步?}
    I -->|否| J[完成]
    I -->|是| K[点击同步到物理表]
    K --> L{冲突检测}
    L -->|无冲突| M[直接同步]
    L -->|有冲突| N[展示冲突列表]
    N --> O[用户逐条决策]
    O --> M
    M --> P[写入物理表+同步日志]
```

---

## 流程三：版本管理与定版

```mermaid
flowchart LR
    A[档案详情页] --> B[版本历史]
    B --> C{操作}
    C -->|定版| D[标记已定版, 锁定]
    C -->|回滚| E[选择历史版本]
    C -->|查看日志| F[操作日志列表]
    D --> G[后续修改创建新版本]
    E --> H[恢复数据+新版本快照]
    F --> I[按时间倒序展示]
```

---

## 流程四：MDM 存活裁决与冲突审查（REQ-018）

```mermaid
flowchart TB
    subgraph 业务人员
        A[编辑档案字段保存]
    end
    subgraph 系统
        B[自动登记修正保护 override]
        C[sync-schema 拉数]
        D{逐字段比对源值 vs 档案值}
        E[值一致: 跳过]
        F[生成冲突项+建议裁决入队]
        G[同步结果提示 N 条冲突待审查]
    end
    subgraph 数据管理员
        H[冲突审查Tab 逐项/批量裁决]
        I{裁决}
        J[接受源值: 更新档案+版本快照+解除保护]
        K[保留档案值: 登记/维持修正保护]
        L[手动触发字段级回写: 预检差异→勾选字段→执行]
    end
    A --> B
    C --> D
    D -->|一致| E
    D -->|差异| F
    F --> G
    G --> H
    H --> I
    I -->|接受源值| J
    I -->|保留档案值| K
    K --> L
    L -->|仅勾选字段| M[源系统物理表]
```

**异常路径**：同一字段重复冲突→旧项自动作废只留最新；回写失败→失败字段明细进同步日志不影响其他字段；裁决变更无版本快照→禁止提交。

---

## 数据流标注

| 操作 | 数据来源 | 数据去向 | 事务边界 |
|------|----------|----------|----------|
| 创建档案 | modeling.StandardField | archive.Archive（配置） | 单事务 |
| 新增记录 | 用户表单 | archive.ArchiveRecord（本地） | 单事务（记录+版本+日志） |
| 编辑记录 | 用户修改 | archive.ArchiveRecord（本地）+ 修正保护标记 | 单事务（版本+日志+保护登记） |
| 同步拉数（比对） | 源物理表 | 冲突队列（差异）/ ArchiveRecord（新增记录） | 分批事务 |
| 冲突裁决 | 冲突项 | archive.ArchiveRecord + 版本 + 血缘 | 单事务（逐项） |
| 字段级回写 | archive.ArchiveRecord（勾选字段） | modeling.DataSource（外部物理表） | 分批事务（每表独立） |
| 定版 | 用户操作 | archive.ArchiveVersion（状态变更） | 单事务 |
| 回滚 | archive.ArchiveVersion | archive.ArchiveRecord（覆盖） | 单事务（新版本+日志） |
# 业务流程

> 系统边界：主数据管理平台
> 外部系统：企业微信、AI大模型API

---

## 流程一：主数据建模全流程

**涉及角色**：数据管理员

```mermaid
flowchart TB
    subgraph 建模流程
        direction TB
        A([开始]) --> B[创建域]
        B --> C[在域下创建实体表]
        C --> C1{表类型}
        C1 -->|数据源表| C2[配置外部库连接信息\n拉取外部表结构]
        C1 -->|本地数据表| C3[输入表名称/编码/描述\n系统本地建表]
        C2 --> D[配置字段名称\n及基础信息]
        C3 --> D
        D --> E[AI分析: 字段分类+冗余检测]
        E --> F{审核AI结果}
        F -->|满意| G[确认分类/排除冗余]
        F -->|需调整| H[拖拽调整分组\n确认或排除冗余标记]
        H --> G
        G --> I[字段属性配置界面\n类型/长度/必填/默认值/枚举值/校验]
        I --> J{需要配置字段映射?}
        J -->|是| K[配置字段映射关系\n支持联合主键]
        J -->|否| L[模型生效]
        K --> L
        L --> M[模型上线]
        M --> N[导出模型文档\nPDF/Markdown]
    end

    subgraph 模型维护
        N[新增字段] --> I
        O[修改字段属性] --> P[存量数据兼容检查]
        P --> Q{兼容?}
        Q -->|是| R[允许修改]
        Q -->|否| S[阻止并提示冲突]
        R --> I
        T[作废字段] --> U[确认作废]
        U --> V[字段隐藏·存量保留]
    end
```

**数据流标注**
| 步骤 | 数据输入 | 数据输出 | 存储 |
|------|----------|----------|------|
| 创建域 | 域名称/编码/描述 | 域元数据 | 元数据仓库 |
| 建表（数据源） | 外部库连接+表名 | 引入的表结构元数据 | 元数据仓库 |
| 建表（本地） | 表名称/编码/描述 | 表元数据+物理表 | 元数据仓库+数据库 |
| 配字段 | 字段名称列表 | 基础字段元数据 | 元数据仓库 |
| AI分析 | 字段名称+类型+语义 | 分类建议+冗余标记 | 元数据仓库 |
| 字段属性配置 | 类型/长度/枚举/校验 | 完整字段元数据 | 元数据仓库 |
| 字段映射 | 源字段→目标字段 | 关系元数据 | 元数据仓库 |

---

## 流程二：档案维护流程

**涉及角色**：业务人员、数据管理员、系统管理员、审计员

```mermaid
flowchart TB
    subgraph 入口
        A([用户登录]) --> B[选择目标域]
        B --> C[进入档案列表页]
        C --> D[列表展示该域下所有档案\n支持搜索/筛选/分页]
    end

    subgraph 角色操作
        D --> E{根据角色权限}
        E -->|查看权限| F[点击查看详情]
        E -->|编辑权限| G[点击编辑]
        E -->|新增权限| H[点击新增]
        E -->|删除权限| I[点击删除]
        E -->|审计权限| J[查看操作日志]
        E -->|接口管理权限| K[配置API接口]
    end

    subgraph 详情与版本
        F --> L[分组展示档案详情]
        L --> M[查看变更历史]
        M --> N[版本列表]
        N --> O[版本差异对比]
        N --> P[回滚到指定版本]
        P --> Q[生成回滚版本快照]
    end

    subgraph 数据操作
        G --> R[进入编辑模式]
        R --> S[提交保存]
        S --> T[生成版本快照]
        T --> U[记录操作日志]
        H --> V[按分组填写表单]
        V --> T
        I --> W[确认删除]
        W --> T
    end

    subgraph 批量与导出
        D --> X[导出Excel]
        D --> Y[导入Excel]
        Y --> Z[下载模板→填写→上传]
        Z --> AA[逐行校验]
        AA --> AB{校验结果}
        AB -->|全部通过| AC[批量写入→生成版本批次]
        AB -->|有错误| AD[展示错误报告]
        AD --> AE[修正后重新导入]
    end

    subgraph 权限与接口
        J --> AF[审计日志页面\n筛选/查看变更详情/导出]
        K --> AG[API配置页面\n开放字段/认证方式/密钥管理]
    end

    U --> D
    Q --> D
    AC --> D
```

**数据流标注**
| 步骤 | 数据输入 | 数据输出 | 存储 |
|------|----------|----------|------|
| 选择域 | 域ID | 该域档案列表 | 元数据+档案数据库 |
| 新增/修改 | 分组表单数据 | 档案记录+版本快照 | 档案数据库+版本仓库 |
| 删除 | 记录ID | 删除标记 | 档案数据库（软删除） |
| 导入 | Excel文件 | 批量记录+版本批次 | 档案数据库+版本仓库 |
| 导出 | 筛选条件 | Excel文件 | 文件下载 |
| 版本操作 | 版本ID | 版本快照/差异对比 | 版本仓库 |
| 日志查询 | 筛选条件 | 操作日志列表 | 日志数据库 |

---

## 流程三：质量规则引擎流程（AI驱动）

**涉及角色**：数据管理员、AI大模型、企业微信

```mermaid
flowchart TB
    subgraph 规则入口
        A([数据管理员登录]) --> B[进入目标域]
        B --> C[切换至质量规则Tab]
        C --> D[展示该域已有规则列表]
        D --> E{操作选择}
    end

    subgraph 单条AI配置
        E -->|新增AI规则配置| F[进入AI对话框]
        F --> G[用户输入自然语言描述]
        G --> H[AI展示转译逻辑\n步骤1: 系统理解\n步骤2: 技术语义\n步骤3: 规则表达式]
        H --> I[用户审核]
        I --> J{认可?}
        J -->|是| K[配置执行频率/通知对象]
        K --> L[规则启用]
        J -->|否| M[修改描述重新生成]
        M --> H
    end

    subgraph 批量创建
        E -->|批量创建规则| N{已有规则?}
        N -->|有| O[按钮置灰·提示已有规则]
        N -->|无| P[AI分析域模型结构]
        P --> Q[生成建议规则列表]
        Q --> R[用户逐条点击查看详情]
        R --> F
    end

    subgraph 规则执行
        L --> S[规则调度执行]
        S --> T{检查结果}
        T -->|全部通过| U[无操作]
        T -->|发现违规| V[记录违规详情]
        V --> W[推送企业微信通知]
    end

    subgraph 质量总结
        X[每日定时触发] --> Y[AI汇总24h质量数据]
        Y --> Z[生成质量日报]
        Z --> AA[推送企微卡片给管理者]
        AA --> AB[Web端查看完整质量报告\n统计/趋势图/违规明细]
    end

    subgraph 对话追溯
        L --> AC[查看AI配置记录]
        AC --> AD[展示完整AI对话历史]
    end
```

**数据流标注**
| 步骤 | 数据输入 | 数据输出 | 存储/外部 |
|------|----------|----------|-----------|
| AI语义配置 | 自然语言描述 | 规则表达式+转译记录 | AI大模型API+规则仓库 |
| AI批量生成 | 域模型元数据 | 推荐规则集 | AI大模型API |
| 规则执行 | 规则定义+档案数据 | 检查结果（通过/违规） | 规则结果仓库 |
| 企微通知 | 违规数据摘要 | 企微消息卡片 | 企业微信 |
| 质量日报 | 24h检查结果 | 日报内容+建议 | AI大模型API+企微 |

---

## 流程四：权限与接口管理流程

**涉及角色**：系统管理员、外部系统

```mermaid
flowchart TB
    subgraph 权限管理
        A([系统管理员登录]) --> B[进入权限管理]
        B --> C[创建角色]
        C --> D[分配角色权限\n角色×档案×操作]
        D --> E[用户分配到角色]
        E --> F[权限生效]
    end

    subgraph 接口管理
        G[进入接口配置] --> H[选择目标档案]
        H --> I[配置开放字段和操作]
        I --> J[配置认证方式]
        J --> K[生成API密钥]
        K --> L[发布接口文档]
    end

    subgraph 审计
        M[审计员登录] --> N[审计日志页面]
        N --> O[筛选/搜索日志]
        O --> P[查看变更详情]
        P --> Q[导出日志报表]
    end
```

---

## 流程五：计算字段配置与执行

```mermaid
flowchart TB
    subgraph 配置阶段["数据管理员 - 计算字段配置"]
        A[字段管理→计算分类] --> B[新建计算字段]
        B --> C[填写编码/名称]
        C --> D[编写Excel风格公式]
        D --> E[系统自动解析依赖]
        E --> F{循环依赖?}
        F -->|是| G[报错: 高亮循环链路]
        G --> D
        F -->|否| H[展示依赖关系+执行顺序]
        H --> I[枚举试算验证]
        I --> J{结果正确?}
        J -->|否| D
        J -->|是| K[确认保存]
    end

    subgraph 执行阶段["系统 - 自动重算"]
        L[数据源同步完成] --> M[按DAG拓扑排序]
        M --> N[批量重算所有计算字段]
        N --> O[结果写入档案记录]

        P[用户编辑保存记录] --> Q{变更字段被依赖?}
        Q -->|是| R[实时重算相关计算字段]
        R --> S[结果随记录持久化]
        Q -->|否| T[不触发重算]
    end
```

### 流程说明

| 阶段 | 触发条件 | 数据流 |
|------|----------|--------|
| 配置 | 数据管理员主动操作 | 基础/组合字段元数据 → 公式定义 → DAG依赖图 |
| 批量重算 | sync-schema 完成后自动触发 | 档案记录基础字段值 → 公式引擎 → 计算结果落库 |
| 实时重算 | 用户编辑保存时自动触发 | 变更字段 → 依赖检查 → 重算受影响字段 |

---

## 系统边界与外部集成总图

```mermaid
flowchart LR
    subgraph 主数据系统边界
        direction TB
        A[主数据建模引擎]
        B[档案维护模块]
        C[质量规则引擎]
        D[权限与接口模块]
    end

    subgraph 外部
        E[AI大模型API]
        F[企业微信]
        G[外部下游系统]
        H[BI/报表系统]
    end

    C -- 语义规则/批量推荐/日报 --> E
    C -- 违规通知/日报推送 --> F
    D -- 开放API --> G
    B -- 导出报表 --> H
```

---

## 异常路径汇总

| 流程 | 异常场景 | 处理方式 |
|------|----------|----------|
| 建模 | 域编码重复 | 提示已存在，禁止创建 |
| 建模 | 字段变更不兼容存量数据 | 阻止修改并列出冲突记录 |
| 建模 | AI分类API超时 | 降级为平铺展示，提示"分类暂不可用" |
| 档案维护 | 并发修改同一记录 | 乐观锁，后提交者提示冲突 |
| 导入 | 超过单次导入行数上限 | 提示拆分导入，建议异步处理 |
| 规则执行 | 企微推送失败 | 记录失败日志，自动重试3次 |
| 规则执行 | AI API断连 | 规则自动降级为延迟检查，AI功能提示不可用 |
| 权限 | 用户无操作权限 | 按钮置灰/API返回403 |
| 计算字段 | 循环依赖 | 阻止保存，高亮循环链路 |
| 计算字段 | 公式语法错误 | 实时标红错误位置，阻止保存 |
| 计算字段 | 重算失败（运行时） | 值置null + 记录错误日志，不阻塞主流程 |
| 计算字段 | 被依赖字段停用/删除 | 依赖方标记异常状态，提示用户修复 |
| 计算字段 | 试算时无去重缓存 | 提示先刷新去重数据 |
| 计算字段 | 笛卡尔积组合过多（>100） | 截断并提示“组合过多，建议手动输入” |
